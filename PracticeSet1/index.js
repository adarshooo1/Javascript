const rectangleBox = document.getElementById("rect");

// If we doing something related to mouse move Movement then do not take any argument inside the function
rectangleBox.addEventListener("mousemove", function () {
  console.log("Hei");
});
